server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_clockshop
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO

jwt:
  secret: clock_shop_secret
  expiration: 86400000